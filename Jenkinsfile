pipeline {
       agent any

       stages {
           stage('Checkout') {
               steps {
                   
                   bat 'php index.php'
               }
           }
           
         }

}
